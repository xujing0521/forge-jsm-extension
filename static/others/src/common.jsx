// Shared bootstrap for custom-ui entries. Replace this stub with your own mount logic
// (e.g. ReactDOM.createRoot(...).render(<App />)) when wiring up your UI.
export function mount(_App) {
  // no-op placeholder
}
